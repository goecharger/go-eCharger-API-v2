## Englisch

Added api key to override all LED colors

Add api keys to show mqtt client/server key/cert metadata

ocpp/mqtt key/cert are now verified when set

Add api keys to show ocpp key/cert metadata

add debugging api key ss for network debugging

Use "mfw" as API key for reporting "cellular modem firmware version"

Fixes invaild GetCofiguration response when the payload is larger than internal memory, and adds optional pagination parameter: offset, limit.

Implements OCPP extension to set/change several API keys via GetConfiguration/ChangeConfiguration.


## German


API Key hinzugefügt um alle LED farben zu überschreiben

Api keys hinzugefügt um mqtt client/server key/cert metadata anzuzeigen

ocpp/mqtt key/cert werden nun beim Setzen verifiziert

Api keys hinzugefügt, um ocpp key/cert metadata anzuzeigen

api key ss hinzugefügt für besseres netzwerk debugging

Verwende "mfw" als API-Schlüssel für die Meldung der Firmwareversion des Mobilfunkmodems.

Behebt ungültige GetConfiguration Antwort wenn die Nutzdaten internen Speicher übersteigt, und fügt optionale Paginierungsparameter hinzu: offset, limit.

Implementiert OCPP Erweiterung um etliche API Keys mittels OCPP GetConfiguration/ChangeConfiguration zu lesen oder verändern.
